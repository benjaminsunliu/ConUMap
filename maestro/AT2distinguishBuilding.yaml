appId: host.exp.Exponent
---
# Requires EXPO_APP_URL to be set (e.g. in Maestro .env) before running this flow.
- openLink: ${EXPO_APP_URL}
- tapOn: "SGW"
- assertVisible:
   label: "Concordia building FB"
- assertVisible:
   id: "e2e-marker-FB"